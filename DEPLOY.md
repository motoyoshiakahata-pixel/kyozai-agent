# デプロイ手順（はじめての方向け）

このアプリを、インターネット上で自分だけが使える状態にするまでの手順です。プログラミングの知識は不要です。**上から順にそのまま進めてください。**

所要時間の目安は初回1〜2時間です。途中で分からなくなったら、そのステップ番号を伝えてください。

---

## 0. 全体像

やることは4つだけです。

| | やること | どこで |
| --- | --- | --- |
| A | AIに問題を作らせるための鍵をもらう | Anthropic |
| B | Googleドライブを読み書きする許可証を作る | Google Cloud |
| C | アプリをインターネット上に置く | Vercel |
| D | AとBをCに登録して、つなぐ | Vercel |

**費用**: Vercelは無料枠、Google Cloudは無料です。Anthropic APIだけ従量課金で、Claude Proを契約していれば月20ドル相当のクレジットが使える場合があります（後述）。

**用語**:
- **環境変数**: パスワードや鍵を、プログラムの外側から安全に渡すための設定欄です。Vercelの管理画面で入力します。
- **デプロイ**: 書いたプログラムをインターネット上に置いて、動く状態にすることです。

---

## 1. 準備：どのGoogleアカウントを使うか

このアプリは2種類のGoogleアカウントを使い分けます。**ここを間違えると先に進めなくなるので、最初に確認してください。**

| 用途 | 使うアカウント | 理由 |
| --- | --- | --- |
| Google Cloudでの設定作業（ステップ3） | **個人のGoogleアカウント**（@gmail.com など） | 学校アカウントはGoogle Cloudの利用が管理者によって禁止されており、「アクセス権がありません」と表示されるため |
| 完成したアプリへのログイン | **学校のGoogleアカウント**（@gse.okayama-c.ed.jp） | 教科書PDF・資料集PDFなどの教材が、このアカウントのドライブにあるため |

個人のGoogleアカウントを持っていない場合は、[accounts.google.com](https://accounts.google.com/signup) で無料で作成できます。

> **⚠️ 事前に知っておいてほしいリスク**
> 学校のGoogleアカウントは、管理者の設定によって「外部のアプリからのアクセス」が禁止されている場合があります。その場合、ステップ10のログイン時に「このアプリはブロックされました」と表示されます。こうなったら学校のGoogle Workspace管理者に、このアプリのアクセス許可（許可リストへの登録）を依頼する必要があります。**このステップまで来ないと分からない**ので、まずは進めてみてください。

---

## 2. Anthropic APIキーを取る

AIに文章を作らせるための鍵です。

1. [console.anthropic.com](https://console.anthropic.com/) を開き、アカウントを作成（またはログイン）します。
2. 左メニューの **API Keys** を開きます。
3. **Create Key** を押し、名前は自由（例: `kyozai-agent`）で作成します。
4. 表示された `sk-ant-...` で始まる文字列を、**メモ帳などに貼り付けて保存**します。
   - **この画面を閉じると二度と表示されません。** 必ず先にコピーしてください。
   - 誰にも見せないでください。この鍵があれば誰でもあなたの残高でAIを使えてしまいます。

> Claude Proを契約している場合、プログラムからの利用に月20ドル相当のクレジットが適用される場合があります。詳細はAnthropicの案内をご確認ください。適用されない場合は、Anthropicのコンソールで少額（例: 5ドル）をチャージすれば使えます。

---

## 3. Googleドライブの許可証を作る（Google Cloud）

**ここは手順が多いですが、一度きりの作業です。個人のGoogleアカウントでログインして進めてください。**

### 3-1. プロジェクトを作る

1. [console.cloud.google.com](https://console.cloud.google.com/) を開きます。
2. 画面上部の、プロジェクト名が表示されている部分（初回は「プロジェクトを選択」）をクリックします。
3. **新しいプロジェクト** → 名前に `kyozai-agent` と入力 → **作成**。
4. 作成後、画面上部が `kyozai-agent` になっていることを確認します。**（ここが別のプロジェクトのままだと、以降の設定が反映されません）**

### 3-2. 必要な機能をオンにする

1. 左メニュー **APIとサービス** → **ライブラリ**。
2. 検索欄に `Google Drive API` と入力 → 出てきたものをクリック → **有効にする**。
3. もう一度ライブラリに戻り、`Google Forms API` を検索 → **有効にする**。

### 3-3. 同意画面を作る

「このアプリがあなたのドライブにアクセスします」と表示する画面の設定です。

1. 左メニュー **APIとサービス** → **OAuth同意画面**。
2. User Type は **外部** を選び、**作成**。
3. 入力するのは次の3つだけで大丈夫です。
   - アプリ名: `教材作成AIエージェント`
   - ユーザーサポートメール: 自分のメールアドレス
   - デベロッパーの連絡先情報: 自分のメールアドレス
4. **保存して次へ** を、スコープの画面でも何も追加せずに押していきます。
5. **テストユーザー** の画面で **+ ADD USERS**（ユーザーを追加）を押し、**学校のGoogleアカウント**（`motoyoshi_akahata@gse.okayama-c.ed.jp`）を入力して保存します。
   - **ここが最重要です。** ここに登録したアカウントだけがアプリにログインできます。
6. 公開ステータスは **テスト** のままで構いません（自分だけが使うため）。

### 3-4. クライアントID・シークレットを作る

1. 左メニュー **APIとサービス** → **認証情報**。
2. 上部の **+ 認証情報を作成** → **OAuth クライアント ID**。
3. アプリケーションの種類: **ウェブ アプリケーション**。
4. 名前: `kyozai-agent-web`（自由）。
5. **承認済みのリダイレクトURI** に、いったん次の1つだけを追加します。

   ```
   http://localhost:3000/api/auth/callback
   ```

   > 本番用のURLは、アプリをデプロイしないと決まりません。ステップ9で、アプリの画面に表示される正しいURLをコピーして追加します。**今はこれで大丈夫です。**

6. **作成** を押すと、**クライアントID**（`....apps.googleusercontent.com`）と**クライアントシークレット**が表示されます。両方をメモ帳に保存してください。

---

## 4. セッション暗号化キーを作る

ログイン情報を暗号化するための、ランダムな文字列です。

[generate-secret.vercel.app/32](https://generate-secret.vercel.app/32) を開くと、ランダムな文字列が表示されます。それをメモ帳に保存してください。

（意味のある文字列である必要はありません。他人に推測されない長い文字列であれば何でも構いません。）

---

## 5. Vercelにログインする

アプリをインターネット上に置くサービスです。

1. [vercel.com/signup](https://vercel.com/signup) を開きます。
2. **Continue with GitHub** を押して、GitHubアカウントでログインします。

### 「Looks like something went wrong!」と出る場合

前回ここで止まりました。次の順に試してください。**上から順に、1つ試すごとにログインし直してみてください。**

1. **シークレットウィンドウをやめて、通常のウィンドウで開く**
   Vercelのログインはブラウザにデータを保存する必要があり、シークレットモードだと失敗することがあります。
2. **広告ブロッカー・トラッキング防止を一時的にオフにする**
   ブラウザの拡張機能（uBlock Origin、AdGuardなど）や、Braveのシールド、Safariの「サイト越えトラッキングを防ぐ」が原因になります。
3. **別のブラウザで試す**
   Google Chromeが最も確実です。
4. **Vercelのクッキーを削除してから再試行**
   ブラウザの設定 → プライバシー → Cookieとサイトデータ → `vercel.com` を検索して削除。
5. **GitHubログインをあきらめて、メールでログインする** ←効果が高い回避策
   [vercel.com/login](https://vercel.com/login) を開き、**Continue with Email** を選びます。メールアドレスを入力すると確認メールが届くので、そのリンクを開けばログインできます。
   GitHubとの連携は、ログイン後に **Settings → Git → Connect GitHub** から改めて行えます（この経路のほうが成功しやすいです）。

それでも解決しない場合は、表示されているエラーメッセージのスクリーンショットを送ってください。別の方法（別サービスへのデプロイなど）を検討します。

---

## 6. リポジトリをVercelに取り込む

1. Vercelにログインした状態で **Add New... → Project** を押します。
2. GitHubのリポジトリ一覧から `kyozai-agent` を探し、**Import** を押します。
   - 一覧に出てこない場合は **Adjust GitHub App Permissions** から、このリポジトリへのアクセスを許可してください。
3. **Framework Preset** が `Next.js` になっていることを確認します（自動で選ばれます）。
4. **まだDeployを押さないでください。** 先に次のステップで環境変数を入力します。

---

## 7. 環境変数を入力する

Importの画面に **Environment Variables** という欄があります（見当たらない場合は、先にDeployしてから **Settings → Environment Variables** で設定できます）。

次の4つを、Key（名前）とValue（値）の組で入力します。**名前は1文字も間違えないよう、コピーして貼り付けてください。**

| Key（名前） | Value（値） |
| --- | --- |
| `ANTHROPIC_API_KEY` | ステップ2で保存した `sk-ant-...` |
| `GOOGLE_CLIENT_ID` | ステップ3-4のクライアントID |
| `GOOGLE_CLIENT_SECRET` | ステップ3-4のクライアントシークレット |
| `AUTH_SECRET` | ステップ4で作ったランダムな文字列 |

> `GOOGLE_REDIRECT_URI` と `GOOGLE_DRIVE_MCP_SERVER_URL` は**設定不要**です。アプリが自分のURLを自動で判別します。

---

## 8. デプロイする

**Deploy** ボタンを押します。3〜5分ほど待つと完了し、`https://kyozai-agent-〇〇〇.vercel.app` のようなURLが表示されます。

このURLをブラウザで開いてください。アプリの画面が表示されれば成功です。

---

## 9. リダイレクトURIを登録する

アプリ画面の **「セットアップ状況」** カードに、**Google Cloud Consoleに登録するリダイレクトURI** が表示されています。

1. その横の **コピー** ボタンを押します。
2. [Google Cloud Console](https://console.cloud.google.com/) → **APIとサービス** → **認証情報** → ステップ3-4で作ったOAuthクライアントIDをクリック。
3. **承認済みのリダイレクトURI** の **+ URIを追加** を押し、コピーしたURLを貼り付けます。
4. **保存** を押します。
5. 反映まで数分かかることがあります。すぐにログインできなくても、5分ほど待ってから試してください。

---

## 10. 動作確認

1. アプリのURLを開き、**Googleでログイン** を押します。
2. **学校のGoogleアカウント**（`motoyoshi_akahata@gse.okayama-c.ed.jp`）を選びます。
3. 「このアプリはGoogleで確認されていません」と警告が出たら、**詳細** → **（安全ではないページ）に移動** を押します。
   - 自分で作ったアプリなので問題ありません。テストモードのため、この警告が出ます。
4. ドライブへのアクセス許可を **許可** します。
5. ログイン後、**参照フォルダ** カードの4つのフォルダすべてに「接続OK」と表示されることを確認します。
6. チャット欄に次のように入力して送信します。

   ```
   ②他者とともに生きる人間としてのあり方（教科書p.14-21）の単元で、共通テスト形式の問題を3問作成してください
   ```

7. まず**作成プラン**が提示されます。内容を確認して **このプランで進める** を押します。
8. 問題案が表示されます。確認して **この内容で保存する** を押します。
9. Googleドライブの **作成教材** フォルダと **12_問題モデルフォルダ** にファイルができていれば完成です。

---

## よくあるエラーと対処

| 症状 | 原因と対処 |
| --- | --- |
| `redirect_uri_mismatch` と表示される | ステップ9のリダイレクトURI登録が未完了か、URLが1文字違います。アプリ画面のコピーボタンから貼り直してください。末尾のスラッシュの有無も区別されます。 |
| 「このアプリはブロックされました」 | 学校のGoogle Workspace管理者が外部アプリを禁止しています。管理者への依頼が必要です（ステップ1の注意書き参照）。 |
| 「アクセスをブロック: このアプリのリクエストは無効です」 | ステップ3-3のテストユーザーに、ログインしようとしているアカウントが登録されていません。 |
| 画面に「セットアップ状況：未完了」と出る | 表示されている環境変数がVercelに未登録です。Settings → Environment Variables で追加し、**Deployments → 最新のデプロイ → Redeploy** を押してください（環境変数は再デプロイしないと反映されません）。 |
| 参照フォルダが「見つかりません」 | フォルダを移動・削除した可能性があります。`src/lib/drive-folders.ts` のフォルダIDを更新してください。 |
| 教材作成が60秒で止まる | Vercel無料枠の実行時間上限です。ページ範囲を狭めて指示するか、`src/lib/anthropic.ts` の `CHAT_EFFORT` を `low` に下げてください。 |
| Anthropic APIの認証に失敗しました | `ANTHROPIC_API_KEY` が間違っているか、残高がありません。Anthropicのコンソールで確認してください。 |

---

## 補足：あとから設定を変えたいとき

- **環境変数を変えたら必ず再デプロイ**が必要です（Deployments → ⋯ → Redeploy）。
- コードを変更してGitHubにプッシュすると、Vercelが自動的に再デプロイします。
